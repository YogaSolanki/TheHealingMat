# Healing Mat API

Nest.js backend for The Healing Mat. PostgreSQL is configured through TypeORM.

```bash
cp .env.example .env
npm install
npm run start:dev
```

The API listens on `http://localhost:4000/api`. Health check: `GET /api/health`.
